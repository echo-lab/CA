# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
/cache/
server/cache/tts/
server/cache/image/
logs/

# Runtime session log — generated/appended while the app runs (e.g. on the VM).
# Already untracked; ignored here so its live churn never re-enters git or causes
# modify/delete conflicts. The file stays on disk; export the data out-of-band.
server/session-log.csv

# production
/build

# misc
.DS_Store
**/.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Project specific
react-text-to-speech/server/key/
key/

# Ignore all files with the following extensions
.key
.csr
.pem
